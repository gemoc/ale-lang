behavior test.lookup.simple;

open class A {

	@main
	def String main(A arg){
		result := self.foo(arg);
	}
	
	def String foo(B arg) {
		result := 'a.foo(b)';
	}
	
}

open class B {

	def String foo(A arg) {
		result := 'b.foo(a)';
	}
}
