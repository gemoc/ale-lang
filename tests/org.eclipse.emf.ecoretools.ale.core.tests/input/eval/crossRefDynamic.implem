behavior test.containattributes;

open class ClassA {
	test::ClassA crossRef;
		
	@main
	def Sequence(ecore::EObject) main(test::ClassA arg) {
		self.crossRef := arg;
		result := self.eCrossReferences();
	}
}
